<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clinicalresearch.clinicalresearch">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".screen_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".screen_projectlanding" />
        <activity android:name=".screen_tasks" />
        <activity android:name=".screen_projectlisting1" />
        <activity android:name=".screen_setting" />
        <activity android:name=".splash_profile" />
        <activity android:name=".screen_create"></activity>
    </application>

</manifest>